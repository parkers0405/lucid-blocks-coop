Current default avatar:
- `rigged_default/low_poly_character.glb`

Drop the next rigged avatar asset here when ready if you want to replace it.

Recommended starting point:
- `GLB` format
- one humanoid armature
- one visible skinned mesh
- textures embedded or shipped next to the model

The mod currently ships one bundled rigged default avatar and falls back to a simple built-in placeholder if that import fails.
